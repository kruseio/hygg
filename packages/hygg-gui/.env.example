# Optional: point a self-host / LAN build at your own hygg-server instead of the
# SaaS default. Copy to `.env` (gitignored); build.rs bakes it in via option_env!.
# HYGG_GUI_SERVER_URL=http://192.168.1.20:8080
