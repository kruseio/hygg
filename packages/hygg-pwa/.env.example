# Copy to packages/hygg-pwa/.env to point a local / self-host dev PWA build at your own
# server. build.rs bakes this into the wasm; unset → the committed hosted default.
HYGG_PWA_SERVER_URL=http://YOUR_LAN_IP:3032
