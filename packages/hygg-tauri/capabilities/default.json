{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capabilities. The reader loads its file via the webview's own file input and hands the bytes to the extract_document IPC command, so no extra plugin permissions are needed.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
