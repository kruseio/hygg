target
*/target
test-data/local
progress.json
TODO.md
packages/hygg-server/data/
# A SQLite DB is also created here if the server is run from the repo root
# (e.g. `cargo run -p hygg-server`) instead of from packages/hygg-server/.
/data/
packages/hygg-server/.env
test-upload.txt
AGENTS.md
.DS_Store

# hygg-pwa: Trunk build output + Playwright MCP scratch (screenshots/fixtures)
packages/hygg-pwa/dist/
.playwright-mcp/
packages/hygg-pwa/.env
